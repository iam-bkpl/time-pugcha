## Installation

```bash
brew tap iam-bkpl/time-pugcha
brew install time-pugcha
```

### macOS Security Warning
If you see a Gatekeeper warning, run:
```bash
xattr -dr com.apple.quarantine $(which time-pugcha)
```
